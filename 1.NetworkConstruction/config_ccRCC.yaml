# EcoNet Network Construction, ccRCC (CCRCC EcoTyper model, RE1-RE11).
# Edit the input paths, then:
#   python run_pipeline.py --config config_ccRCC.yaml

scrna_h5ad: "PATH/TO/your_scRNA.h5ad"          # <-- EDIT: log-normalized AnnData
nichenet_dir: "PATH/TO/NicheNet_DB"            # <-- EDIT: NicheNet CSVs (see README)
ecotyper_dir: "PATH/TO/EcotyperModel"          # <-- EDIT: EcoTyper output (Cell_States/ + Ecotypes/)
ecotype_abundance: "PATH/TO/ecotype_abundance.txt"   # <-- EDIT: ecotypes x samples

output_dir: "output_ccRCC"

# .obs columns in the h5ad
cell_state_column: "CellType_State"
sample_column: "Sample"

# Pipeline parameters
overexpr_thresh_p: 100
lri_variable_both: false
n_boot: 100
hill_kh: 0.5
hill_n: 1
comm_thresh_prob: 0
comm_thresh_pval: 0.05
top_ligands_k: 5
expression_threshold: 1
top_mediators_n: 2
n_jobs: -1

# Per-cell-type genes to exclude as noise (set to {} to disable)
noisy_genes:
  CD8.T.cells: [CD4, CD80]
  CD4.T.cells: [CD8A, CD8B]
  Monocytes.and.Macrophages: [CD4, CD8A, CD8B, IL2RA, CD28]
  Epithelial.cells: [CD4, CD8A, CD8B, CD81, LCK, IL7R]
  Endothelial.cells: [CD4, CD8A, CD8B]
  B.cells: [CD4, CD8A, CD8B, CD2, CD247, CD28, CD3E, CD40, CD47, CD72, CCR7, IL2RA, ITGA4, KLRD1, KLRK1, LCK, ITK, NOTCH1]
  Dendritic.cells: [CD4, CD8A, CD8B, CD2, CD226, CD3D, CD3E, CD28, CD40, IL2RG, TGFBR2]
  NK.cells: [CD4, CD8A, CD8B, CD3E, CD3G, CD40, HLA-DPA1]
  PCs: [CD4, CD8A, CD8B, CD2, CD244]
